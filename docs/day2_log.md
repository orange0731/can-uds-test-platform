# Day 2 学习日志（日期：今天）

## 今天跑通了什么
- 把一次性脚本重构成面向对象的 VirtualECU 类
- 实现 start() 主循环：ECU 持续监听总线，不再处理一次就退出
- 用字典实现 SID 服务分发表（0x22 读数据服务）
- 实现完整的正响应（SID+0x40）与负响应（0x7F+SID+NRC）机制
- 编写 4 组验证用例全绿：1 个正响应 + 3 个负响应（NRC 0x31/0x11/0x13）

## 踩过的坑

## 学到的知识点
- 服务分发用字典（SID→处理函数）而不是 if-elif：新增服务只需注册一行，符合开闭原则
- NRC 0x13（长度错误）与 NRC 0x31（DID不存在）的触发场景区别
- python-can 的 virtual 总线只在同一进程内互通，所以 ECU 和 Tester 要用线程共存

## 明天计划
- Day 3：给 ECU 加状态——0x10 会话控制 + 0x27 安全访问（种子&密钥）
